# Orienteering Solver

This package contains C++ implementations of algorithms for the orienteering
problem, exposed to Python via pybind11. It is built with scikit-build / CMake.

Used for development only; see top-level README for project details.
