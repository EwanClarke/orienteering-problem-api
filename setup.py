
from setuptools import setup, find_packages

setup(
    name='orienteering_project',
    version='0.1.0',
    packages= ["orienteering_solver", "orienteering_prep"],
    # Other metadata like install_requires for libraries like fastapi, etc.
)