[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "orienteering-prep"
version = "0.1.0"
description = "Utilities for preparing orienteering inputs"
authors = [ { name = "Ewan Clarke" } ]
readme = "README.md"
requires-python = ">=3.8"

